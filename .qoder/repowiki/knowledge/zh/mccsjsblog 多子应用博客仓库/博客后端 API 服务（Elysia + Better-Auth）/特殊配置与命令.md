运行前需复制 `.env.example` 到 `.env` 并配置 `DATABASE_URL`、`FRONTEND_URL`、`ADMIN_URL`、`BASE_URL`、`BETTER_AUTH_SECRET`；首次启动自动执行 Prisma migration（`pnpm prisma:migrate`），随后由 `seedDefaultMenus` / `seedDefaultFriends` 写入初始数据。开发用 `pnpm dev`（bun --watch），构建产物输出至 `dist/`。