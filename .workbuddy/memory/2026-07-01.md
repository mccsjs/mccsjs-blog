# 2026-07-01

## 代码审查 + 修复
- 审查他人提交的代码（全项目），发现并修复了 7 个问题
- **Prisma Schema**：清理了 Account/Session/Verification 中的重复字段（snake_case + camelCase 两套并存）
- **frontend lucide-react**：版本 `^1.22.0`（不存在）改为 `^0.511.0`
- **backend excerpt**：从 `min(1)` 改为 `optional()`，对齐管理端
- **添加缺失依赖**：`@better-auth/prisma-adapter` 添加到 backend/package.json
- **单用户注册限制**：在 auth.ts 中使用 better-auth databaseHooks 拦截，已有用户时拒绝注册
- **移除死代码**：删除 VditorEditor.tsx + vditor 依赖（已换成 SplitMarkdownEditor）
- **shared 包入口**：module 路径从 `index.ts` 改为 `src/index.ts`
- 重置了开发数据库（因为有迁移冲突），新建了 cleanup_duplicate_fields 迁移

## 当前运行状态
- 后端: http://localhost:4000 (Bun + Elysia)
- 前端: http://localhost:4321 (Astro)
- 管理端: http://localhost:5173 (Vite + React)

## 导航栏复刻 + 图标库替换
- 参照 D:\mccsjs\ming 的导航栏风格，重写了 `frontend/src/components/Header.astro`
  - 浮动胶囊 (pill) 形状，圆角 50px，不贴屏幕边
  - 滚动 > 80px 自动收缩（宽度/内边距/Logo 淡出）
  - hover 滑动边框（完整复刻 ming：纯边框无背景、9999px 圆角、cubic-bezier 过渡、活动项 URL 匹配、mouseleave 吸附回活动项、AbortController 清理）
  - 桌面端菜单绝对居中（left: 50% + translateX）
  - 下拉菜单动画 + 毛玻璃面板
  - 移动端右侧滑入抽屉（1024px 以下触发），菜单项级联滑入
  - 自带 CSS（内联 `<style>`），不依赖额外 CSS 文件
  - 支持 light/dark 模式（通过 `prefers-color-scheme` 自动切换 .dark 类）
- 修改 `BaseLayout.astro`：添加 dark mode 检测脚本
- 修改 `global.css`：添加 View Transitions CSS（备用，Auth 7 导入有 bug 暂未启用）
- 替换图标库：lucide-react → @iconify/react（admin 中 10 个文件）

## 图标渲染修复
- **admin/Menus.tsx**：菜单列表中图标从 `<span>{item.icon}</span>` 改为 `<Icon icon={item.icon} />` 正确渲染
- **frontend/MenuIcon.astro**：重写为支持三种格式（图片URL / emoji / Iconify 标识符）
  - Iconify 用 `iconify-icon` Web Component 渲染（已安装 `iconify-icon` 包）
  - 自动检测：`http(s)://` 或 `/` 开头 → img；`集合:名称` 格式 → iconify-icon；其他 → 纯文本

## 自定义字体
- 引入 `frontend/public/font/b.woff2` 作为默认字体
- 使用 Tailwind v4 `@theme --default-font-family` 全局覆盖，降级链 BlogFont → system-ui → -apple-system → sans-serif

## RSS / Atom / Sitemap 配置
- 安装 `@astrojs/rss` + `@astrojs/sitemap`
- astro.config.mjs 添加 `site: 'https://blog.seln.cn'`、sitemap 集成（filter/changefreq/priority/lastmod）
- 创建 `src/pages/rss.xml.ts`：走 `/api/posts` 拉取数据，用 @astrojs/rss 生成 RSS 2.0
- 创建 `src/pages/atom.xml.ts`：手写 Atom 1.0 XML 模板，含 entry/published/updated/author/category
- 构建产物：`dist/sitemap-index.xml` + `dist/sitemap-0.xml`

## 归档页导航栏重复 Bug 修复
- `archive.astro` 中 Nav tabs 导航栏出现了 3 次（正确位置 + 年份循环内 + renderPage JS 模板字符串中）
- 删除年份循环和 JS renderPage 中两处重复的 `<nav role="tablist">`
- 修复后页面只保留顶部一组导航栏

## 页面 Tab 导航栏重构 — Header 风格
- 新建 `frontend/src/components/TabNav.astro` 组件，复刻 Header 导航栏风格：
  - 胶囊形容器（50px 圆角）、毛玻璃背景、hover 滑动边框动画
  - 每个 tab 项 `border-radius: 50px`，hover 时变色
  - 带 `#tab-hover-border` 绝对定位元素，hover 时在 tab 间平滑滑动
  - mouseleave 时吸附回 active 项
  - 暗色模式适配
- 更新 4 个页面使用 `<TabNav>` 替换原来滚菜单 oklch 风格的 nav tabs：
  - `posts/index.astro`、`archive.astro`、`categories/index.astro`、`tags/index.astro`

## 文章目录 TOC 重构为 Butterfly 风格可折叠树形（最终版）
- 参照 D:\blog 的 Butterfly 主题 TOC 实现，将目录从自定义 div 结构改为嵌套 ol/li：
  - `renderTocOl()` 生成 `<ol class="toc-list"><li class="toc-item toc-level-N" data-toc-slug="..."><a class="toc-link">...</a><ol class="toc-child">...</ol></li></ol>`
  - CSS：`.toc-child { display: none }` + `li.toc-item.active > ol.toc-child { display: block }`
  - JS：滚动时 `findCurrentHeading()` 定位当前标题，给对应 `.toc-link` 加 `.active`，向上遍历 DOM 给所有祖先 `li.toc-item` 加 `.active`
  - `requestAnimationFrame` 节流滚动事件（非 IntersectionObserver）
  - 构建命令：`NODE_PATH=node_modules node node_modules/astro/bin/astro.mjs build/dev`
- 修复建构建错误：缺少 `</script>` 标签、Astro frontmatter 中 `return` 不能在函数外

## 夜间模式适配
- **BaseLayout.astro**：localStorage 持久化主题，`astro:before-swap` 保持状态
- **Header.astro**：切换按钮，太阳/月亮图标切换动画（缩放+旋转），localStorage 同步
  - 亮色 → `line-md:moon` | 暗色 → `solar:sun-broken`
- **global.css**：添加 `.dark` 下 post-content 全套暗色规则；补充 `@custom-variant dark` 让 Tailwind v4 的 `dark:` 变体生效
- **全站组件 dark 适配**（共 11 个文件）：
  - Footer, Breadcrumb, PostCard, CopyrightCard
  - index, posts/index, posts/[slug], archive, categories/*, tags/*
  - 卡片：`bg-white` → `dark:bg-[#111]`，`border-gray-200` → `dark:border-gray-800`
  - 文字：`text-gray-*` → `dark:text-gray-*` 对应映射
  - 链接：`text-yellow-600` → `dark:text-yellow-400`
  - body：`bg-white` → `dark:bg-[#0a0a0a]` + `transition-colors duration-300`

## 管理端编辑器布局优化
- `admin/src/components/SplitMarkdownEditor.tsx`：分屏编辑区左右靠边，中间加明确竖向分割线
  - 左侧源码面板：左内边距 `pl-6`，右内边距 `pr-5`
  - 右侧预览面板：左内边距 `pl-5`，右内边距 `pr-6`
  - 中间分割线：加深颜色 + 渐变描边，空内容时也清晰可见
  - 右侧预览为空时显示占位提示（眼睛图标 + 「右侧预览区域」文案），避免白屏一片
  - 两个面板各自 `flex-1`，撑满可用宽度

## 管理端编辑器布局修复（最终版）
- 修复 `SplitMarkdownEditor` 空内容时编辑器整体只占中间一小块的 bug
  - 根容器、分屏行容器、左右面板都补上 `w-full` 或 `w-1/2`
  - 左/右面板从 `flex-1` 改为 `w-1/2`，确保无论内容多少都各占 50% 宽度
  - 同时给 `PostEditor.tsx` 根容器和主编辑区加上 `w-full`，防止被 flex 父元素自动收缩
- 现在未输入内容时，左右两个面板都贴边撑满，中间分割线居中

## 分屏编辑器同步滚动 + 状态栏贴底
- 右侧预览滚动条隐藏（`.scrollbar-hidden`），左侧 textarea 滚动条保留（位于中间位置）
- 左右滚动同步：`syncScroll` 按比例互相跟随，`isSyncingScroll` 防递归
- `PostEditor.tsx` 根容器高度从 `h-[calc(100vh-57px)]` 改为 `h-full`，修复底部留空导致状态栏不贴底的问题

## 分屏编辑器页脚漂移修复
- 根因：AdminLayout 最外层 `flex min-h-screen`，长文章会将容器撑高，`h-full` 子元素高度会跟随扩大
- 修复：PostEditor 进入时通过 effect 将外层容器 `className` 改为 `flex h-screen overflow-hidden`（固定高度），离开时恢复原值
- 标题输入框加上圆角边框（`border border-[#dcdfe6] rounded-lg`）+ 聚焦蓝色高亮，解决无边框时难以定位的问题


## 约定
- 文章编辑保持单页布局，不拆分标签页或多页面

## 导航栏 Logo 修复
- `.nav-logo-text` 移除硬编码 `font-family: ui-monospace`，继承全局 BlogFont 字体
- 移除滚动后 Logo 淡出消失效果，下滑时 Logo 始终可见

## 聚合菜单下拉按钮
- Logo 左侧添加 favicon 图标 + 聚合菜单下拉面板（参照 D:\3iS73M 的 Aggregate.vue）
- 数据源：`/api/menus?type=GROUP`，在 BaseLayout 拉取后传给 Header
- 修复 `#nav-pill overflow:hidden` 裁切下拉面板问题，改为 `overflow: visible`，玻璃层单独裁剪
- 后端添加 `seedDefaultAggregateMenus()` 初始化默认聚合菜单：「我的网站」（个人主页/博客）+「友情链接」（Astro/Elysia/Prisma/Tailwind）
- 数据库为 `prisma/dev2.db`（.env 指定 DATABASE_URL），menu 表仅存在于 dev2.db

## 右侧悬浮组件
- 新建 `frontend/src/components/FloatingBar.astro`：屏幕右侧固定悬浮按钮组
  - **回到顶部**：全局显示，滚动 >200px 后出现（带弹性缩放动画）
  - **查看评论**：仅文章页（postSlug 存在）显示，点击滚动到评论区或页面底部
  - 样式复刻截图风格：圆角方形(12px)、毛玻璃背景、边框阴影、hover 上浮+放大效果
  - 暗色模式适配
- 集成到 `BaseLayout.astro`，通过 props 控制评论按钮显隐

## 管理端编辑器滚动同步 + 页脚置顶
- 去掉右侧预览面板自己的滚动条，改为左侧 textarea 滚动条（位于中间）作为唯一可见滚动条
- 实现左右滚动同步：`textarea` 与右侧预览按滚动比例互相跟随
- 新增 `.scrollbar-hidden` 工具类隐藏滚动条但保留滚动能力
- 修复 `PostEditor.tsx` 根容器高度：从 `h-[calc(100vh-57px)]` 改为 `h-full`，让状态栏真正贴底

## 前端导航栏 favicon 聚合菜单
- 新增 `favicon` + `aggregateMenus` 参数传给 `Header.astro`
- 在 Logo 左侧显示 favicon 图标，右侧保留站点文字
- hover Logo 区域时弹出聚合下拉菜单：
  - 数据源：`/api/menus?type=GROUP`
  - 按管理端聚合菜单的分组渲染，子项两列网格展示
  - 支持图标（图片/Iconify/emoji），带毛玻璃暗色模式适配
  - 仿照 `D:\3iS73M` 的聚合菜单样式
- 聚合菜单图标按钮从 `<a>` 中剥离为独立 `<button>`，点击/悬浮弹出下拉面板，文字链接保持跳转首页
- 修复 Logo 区域图标与文字重叠：`#nav-logo-wrapper` 添加 `gap-2`，图标 28px 移除 border-radius
- 聚合菜单下拉面板 `top` 改为 `calc(100% + 12px)`，不再覆盖导航栏；加 `z-index: 50`
- 手机端适配：html 加 `overflow-x: clip`（后改到页面 wrapper）、图片 `max-width: 100%`、表格自动包裹 `.post-table-wrap`
- 文章目录 TOC 修复：
  - 目录容器 `aside` 为普通 grid 子项，默认 `stretch` 撑满文章高度
  - 内部 `div` 使用 `sticky top-24` 吸顶，在整篇文章滚动范围内保持可见
  - 目录项过多时内部 `max-h-[calc(100vh-7rem)] overflow-y-auto` 独立滚动
  - 新增 IntersectionObserver 目录滚动跟踪高亮
- 文章目录重构为可折叠树形：
  - 提取范围 h1/h2/h3，扁平数组 → 栈算法构建树形结构（h1 包裹 h2，h2 包裹 h3）
  - 有子项的标题显示 ▶ 折叠箭头，点击展开/收起（CSS max-height 过渡动画）
  - 滚动时 IntersectionObserver 自动展开当前阅读章节的所有祖先路径
  - 默认折叠非活动路径，只展开第一个章节及其子项
  - slug 生成改为 Unicode 安全模式（`\p{L}\p{N}`），中文标题正常显示
- 删除文章页封面图展示、作者头像区、点赞按钮
- 复制链接移至版权卡片右上角
