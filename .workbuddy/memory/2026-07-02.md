- 23:30 用户反馈目录样式变了，喜欢之前的截图样式；只应改样式，不应改折叠逻辑
- 23:33 用户明确保留折叠逻辑；已在 frontend/src/pages/posts/[slug].astro 恢复子目录默认折叠，仅给一级目录加黄色背景 pill 样式
- 23:35 用户要求回退所有改动，已恢复原始样式
- 23:40 用户反馈 TOC 刷新前后样式问题仍存在（HTML 相同但效果不同）。定位根因：swup 从非文章页切入时，[slug].astro 的内联脚本不执行，content:replace 钩子不存在。解决方案：将 TOC 初始化逻辑完全移到 BaseLayout.astro 的 `<script>` 中（所有页面都加载），注册 `page:view` 钩子，双重 rAF 确保 DOM 布局完成后再初始化
- 23:43 用户明确贴图说明：刷新前所有目录层级都展开、无 active 高亮；刷新后正常折叠。根因是 swup 切换后 [slug].astro 的 `<style is:global>` 没加载。已将 TOC 样式从 [slug].astro 移到 frontend/src/styles/global.css 中，swup 切换后样式持久可用
- 23:16 继续完成未完成任务：
  1. [slug].astro: 将 is:inline 脚本改为普通 script（让 Astro 管理生命周期），移除未使用的 define:vars，脚本监听 astro:page-load + swup:page-view 事件重新初始化 TOC
  2. BaseLayout.astro: page:view hook 派发 swup:page-view 自定义事件，触发各页面脚本重新初始化
  3. FloatingBar.astro: 两个 is:inline 脚本加防重复初始化 guard（window.__floatingInited / window.__floatingTocInited），移除不必要的 visit:end hook，修复手机端目录面板每次打开都从桌面端重新复制内容（解决 swup 切换后内容过期问题）
