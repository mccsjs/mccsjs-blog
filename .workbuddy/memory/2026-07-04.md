- 08:39 用户回档后目录样式丢失，已按 7月2日记录恢复全部四处改动
- 08:41 手机端目录面板（#float-toc-panel）默认状态可见，给面板 HTML 加 `hidden` 属性兜底，CSS `.open` 用 `display: flex` 覆盖；JS closePanel 延迟 250ms 后重新 hidden（等过渡动画完成）
- 09:00 Twikoo 评论样式在手机端 swup 切换后丢失，根因同 TOC——`<style is:global>` 在 swup 切换时不重新注入。已将 Twikoo 美化样式移到 `global.css`，移除 `TwikooComments.astro` 里冗余的 `<style is:global>` 块
- 09:05 评论按钮 swup 切换后不显示（只在刷新时显示）。根因：`FloatingBar` 在 swup 容器外，prop 条件渲染在切换时不重新执行。修复：模板始终渲染评论按钮；JS 根据路径（`/posts/` 或 `/link`）控制 `.visible` 显隐；`display: none` + `.visible { display: flex }` 解决隐藏按钮占位问题；`updateVisibility()` 统一管理三个按钮显隐
- 09:15 悬浮按钮自适应问题：隐藏按钮（`display: none` + `opacity: 0`）仍占 flex 布局空间。修复：默认 `display: none`，只有 `.visible` 才 `display: flex`
- 09:20 图片下方出现边框线：`<a data-fancybox="gallery">` 包裹图片，`.post-content a` 的 `border-bottom` 导致。修复：加 `.post-content a:has(> img) { border-bottom: none }`
- 09:25 目录位置调整：`mt-8` → `mt-16` → `mt-24` → `mt-36` → `mt-20`，最终用 `mt-20`（5rem）
- 09:30 **目录 sticky 在 grid 布局不可靠的根本解决方案**：纯 CSS `sticky` 在 grid 的 `stretch` 对齐下不起作用（aside 高度被拉伸到跟文章一样）。最终用 JS 实现"伪 sticky"：
  - 页面顶部：目录在文档流里，位置由 `mt-20` 控制
  - 滚动超过触发点：切换为 `position: fixed; top: 100px`，固定在视口
  - 滚回顶部：切回文档流原位
  - 用占位 `div` 防止切换时父容器高度塌陷
  - 关键代码在 `[slug].astro` 的 `initFixedToc` IIFE 里
