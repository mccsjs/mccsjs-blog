# 2026-07-05 工作日志

## 16:22 - 提取 Header.astro inline script
- 从 `Header.astro` 提取 418 行 `is:inline` script → `frontend/public/scripts/header.js`
- Header.astro 从 618 行精简到 199 行（仅保留 Astro 模板）
- 使用 `<script defer src="/scripts/header.js">` 加载
- 所有功能保持不变：导航滚动收缩、hover边框、下拉菜单、移动端抽屉、聚合菜单、主题切换、搜索
- ES6 语法改写为 ES5 兼容（确保 non-module defer 脚本稳定运行）

## 16:25 - 修复文章页标题重复显示
- 原因：`[slug].astro` 传入 `title={post.title | mccsjs}`，BaseLayout 又拼接了一次 `| siteTitle`
- 修复：`[slug].astro` 改为 `title={post?.title || '文章详情'}`，去掉多余后缀
- BaseLayout 第 46 行已有正则清理逻辑，只需传入纯标题即可

## 16:30 - 去除滚动条上下箭头
- 原因：之前设置 `color-scheme: light dark` 让 Windows Chrome 使用系统原生滚动条，导致 `::-webkit-scrollbar-button` CSS 无法隐藏箭头按钮
- 修复：去掉 `html` 的 `color-scheme`，改为纯 WebKit 自定义滚动条；显式定义 `::-webkit-scrollbar-track` 和 `::-webkit-scrollbar-button { display: none; }`
- 保持 `overflow-y: auto` + `scrollbar-gutter: stable`，避免布局跳动
- 滚动条宽度 5px、圆角 thumb、透明轨道，无上下箭头

## 16:40 - SEO 优化（第一批）
- `BaseLayout.astro`: 修复 title 拼接 bug（硬编码 "My Blog" 正则清理无效）
- `BaseLayout.astro`: 新增 `ogType`/`ogImage` props，加入完整 OG + Twitter Card + Canonical 标签
- `[slug].astro`: 传入 `ogType="article"`、`ogImage={post.coverImage}`，加入 `article:published_time` 等标签
- 已有的 JSON-LD（BlogPosting）保持不变

## 16:45 - SEO 优化（第二批：6 个问题修复）
1. **Swup `updateHead: true`**（原 `false`）→ 页面切换后 title/meta 正确更新
2. **所有页面加独立 description**（index/posts/about/archive/categories/tags/link/comments 及动态页）
3. **404 页面加 `noindex, follow`** → 防止搜索引擎索引 404
4. **Breadcrumb 加 BreadcrumbList JSON-LD** → Google 富摘要面包屑路径
5. **Breadcrumb 修复硬编码** → 删除 `title?.replace(/\s*\|\s*mccsjs\s*$/, '')`
6. **PostCard h3 → h2** → 修复 heading 层级跳级（h1 → h2 → h3）

## 16:48 - 开源适配：去掉域名白名单
- `site: "https://blog.seln.cn"` → `site: process.env.SITE_URL || "http://localhost:4321"`（环境变量，别人部署时自行设置）
- `server.allowedHosts: ["d.seln.cn"]` → `server.host: true`（允许所有域名访问，适合开源项目）

## 16:52 - 修复 TOC 刷新才显示样式
- 原因：`[slug].astro` 的 `<script>`（非 inline）被 Astro 编译为模块，Swup 切页后不重新执行
- 修复：将 TOC 滚动高亮、点击跳转、固定定位、阅读进度条、Fancybox 全部移到 `base-layout.js`
- 新增 `initPostToc()`、`initReadingProgress()`、`initFancybox()` 三个函数
- `[slug].astro` 删除 324 行 `<script>`，只保留 `<link>` 和 `<script is:inline src>` 引用
