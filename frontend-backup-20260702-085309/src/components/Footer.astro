---
import type { MenuItem } from '../types';
import MenuIcon from './MenuIcon.astro';

interface Props {
  siteTitle?: string;
  icp?: string;
  footerText?: string;
  menus?: MenuItem[];
}

const { siteTitle = 'My Blog', icp = '', footerText = '', menus = [] } = Astro.props;
const year = new Date().getFullYear();

const visibleMenus = menus.filter((m) => m.visible);
---

<footer class="border-t border-gray-200 bg-white transition-colors duration-300 dark:border-gray-800 dark:bg-[#0a0a0a]">
  <div class="mx-auto max-w-5xl px-6 py-8 text-sm text-gray-500 dark:text-gray-400">
    {visibleMenus.length > 0 && (
      <div class="mb-6 flex flex-wrap items-center justify-center gap-4 border-b border-gray-100 pb-6 dark:border-gray-800">
        {visibleMenus.map((menu) =>
          menu.children?.length ? (
            <div class="flex items-center gap-2">
              <span class="font-medium text-gray-700 dark:text-gray-300"><MenuIcon icon={menu.icon} class="mr-1 size-4" />{menu.label}:</span>
              {menu.children.filter((c) => c.visible).map((child, idx) => (
                <>
                  <a
                    href={child.href || '#'}
                    target={child.target || undefined}
                    class="hover:text-gray-700 dark:hover:text-gray-200"
                  >
                    <MenuIcon icon={child.icon} class="mr-1 size-4" />{child.label}
                  </a>
                  {idx < (menu.children.filter((c) => c.visible).length - 1) && <span>·</span>}
                </>
              ))}
            </div>
          ) : (
            <a
              href={menu.href || '#'}
              target={menu.target || undefined}
              class="hover:text-gray-700 dark:hover:text-gray-200"
            >
              <MenuIcon icon={menu.icon} class="mr-1 size-4" />{menu.label}
            </a>
          )
        )}
      </div>
    )}
    <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
      <span>
        &copy; {year} {siteTitle}。
        {icp && (
          <a href="https://beian.miit.gov.cn/" target="_blank" rel="noopener noreferrer" class="ml-1 hover:text-gray-700 dark:hover:text-gray-200">
            {icp}
          </a>
        )}
      </span>
      {footerText ? (
        <span class="text-center sm:text-right">{footerText}</span>
      ) : (
        <span>使用 Astro + React + Tailwind CSS 构建</span>
      )}
    </div>
  </div>
</footer>
